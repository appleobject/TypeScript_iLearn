let list: array = ["Apple", "Navin", "Adewale", 5, true];

let person: string = "Steve";

let heightInCentimetre: number = 2348.353527;

let artist: string[] = ["Rick", "Wale", "Meek", "Nav", "4", "Tac"];

let row: number[] = [4, 5, 6, 7, 8, 9];

let booltest: boolean[] = [true, false, false];

let tupleTest: [string, number, boolean] = ["FireBoy", 10, true];

enum DoorStart {
  Open,
  Closed,
  Ajar
}

function log(val: any): void {
  console.log(val);
}
